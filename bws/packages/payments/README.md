# Payments (Stripe) — Prototype

- Configure keys in `.env`.
- Serve `create-checkout-session.php` and `webhook.php` over HTTPS in test mode.
- Replace placeholders with real price IDs and publishable key.
